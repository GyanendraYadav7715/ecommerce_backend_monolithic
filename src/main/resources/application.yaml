spring:
  application:
    name: store
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: ${ACTOKEXP}
    refreshTokenExpiration : ${RETOKEXP}
  profiles:
    active: ${ SPRING_PROFILES_ACTIVE }

stripe:
    secretKey: ${STRIPE_SECRET_KEY}
    webhookSecretKey: ${STRIPE_WEBHOOK_SECRET_KEY}